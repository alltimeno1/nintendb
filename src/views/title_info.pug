doctype html
html(lang='en')
  head
    meta(charset='UTF-8')
    meta(http-equiv='X-UA-Compatible' content='IE=edge')
    meta(name='viewport' content='width=device-width, initial-scale=1.0')
    title Nintendb - All about Nintendo Switch
    link(rel='icon' href='../../static/img/favicon.ico' type='image/x-icon')
    link(href='../../style.css' rel='stylesheet')
  body
    #nav
      a(href='/home')
        img(src='../../static/img/logo.png' height='100px')
      ul
        li
          a(href='/home') 홈
        li
          a#current(href='/title') 게임
        li
          a(href='/rank') 랭킹
        li
          a(href='/forum') 커뮤니티
        li
          a(href='/etc') 고객지원
      a#login(href='/login') 로그인/회원가입
    img#banner(src='../../static/img/banner.jpg' width='100%')
    #container
      #detailbox
        img(src=title.image height='100px')
        #details
          h2=title.name
          p 발매일 : 
            span=new Date(title.date).toLocaleDateString()
          p 메타크리틱 평점 : 
            span=`${title.rating} / 100`
          p 가격 : 
            span=title.price.toLocaleString('ko-KR', { style: 'currency', currency: 'KRW' })
          p
            each tag in title.tag.split(',')
              button=tag
      #commentbox 
        h4 한줄평 남기기
        .private
          input(type='text' name='name' placeholder='아이디')
          input(type='password' name='password' placeholder='비밀번호')
        textarea(name='text' placeholder='착한 댓글 부탁드립니다 :)')
        button.submit(type='submit') 등록
        ul.comments
          li.example
            div 아이디
            div 내용
            div 삭제
          li
            div 익명
            div 너무 재밌었어요 추천!!
            div.delete ✖️
          each item in comment 
            li 
              div=item.id 
              div=item.text
              div.delete(id=item._id) ✖️
    #to-top
      | 🔝
    script(type='module' src='../src/js/scroll.js')
    script(type='module' src='../src/js/update_comment.js')
    script(type='module' src='../src/js/delete_comment.js')
